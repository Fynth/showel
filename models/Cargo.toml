[package]
name = "models"
version = "0.1.0"
edition = "2024"

[dependencies]
serde.workspace = true
sqlx = { workspace = true, features = ["sqlite", "postgres", "mysql"] }
url = "2.5.7"

[dev-dependencies]
serde_json.workspace = true
