[package]
name = "driver-mysql"
version = "0.1.0"
description = "MySQL database driver (via sqlx)"
edition = "2024"

[dependencies]
database.workspace = true
sqlx = { workspace = true, features = ["mysql"] }
