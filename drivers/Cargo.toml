[package]
name = "drivers"
version = "0.1.0"
edition = "2024"

[dependencies]
database.workspace = true
models.workspace = true
sqlx = { version = "0.8.6", features = ["sqlite", "runtime-tokio-rustls"] }
tokio = "1.50.0"
